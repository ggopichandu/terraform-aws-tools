# Jenkins

**Plugins:**
* Pipeline stage view
* AnsiColor
* Pipeline Utility Steps
* Nexus Artifact Uploader --> Not uploading files to Nexus
* SonarQube Scanner
* Rebuild

Restart Jenkins once plugins are installed.

Configure aws credentials in Jenkins agent. Configure will normal user not root user.

